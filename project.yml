# generated with XCEProjectGenerator
# https://github.com/XCEssentials/ProjectGenerator
# https://github.com/workshop/struct/wiki/Spec-format:-v2.0
version: 2.1.0
configurations:
  Debug:
    type: "debug"
    overrides:
      IPHONEOS_DEPLOYMENT_TARGET: "8.0"
      SWIFT_VERSION: "3.1"
      VERSIONING_SYSTEM: "apple-generic"
      CURRENT_PROJECT_VERSION: "0"
      CODE_SIGN_IDENTITY[sdk=iphoneos*]: ""
      SWIFT_OPTIMIZATION_LEVEL: "-Onone"
  Release:
    type: "release"
    overrides:
      IPHONEOS_DEPLOYMENT_TARGET: "8.0"
      SWIFT_VERSION: "3.1"
      VERSIONING_SYSTEM: "apple-generic"
      CURRENT_PROJECT_VERSION: "0"
      CODE_SIGN_IDENTITY[sdk=iphoneos*]: ""
targets:
  Fwk:
    platform: "ios"
    type: ":framework"
    sources:
    - "Sources"
    configurations:
      Debug:
        PRODUCT_NAME: "XCEToolbox"
        IPHONEOS_DEPLOYMENT_TARGET: "8.0"
        PRODUCT_BUNDLE_IDENTIFIER: "io.XCEssentials.Toolbox"
        INFOPLIST_FILE: "Info/Fwk.plist"
        ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: "$(inherited)"
        SDKROOT: "iphoneos"
        TARGETED_DEVICE_FAMILY: "1,2"
        DEFINES_MODULE: "NO"
        SKIP_INSTALL: "YES"
        MTL_ENABLE_DEBUG_INFO: "true"
      Release:
        PRODUCT_NAME: "XCEToolbox"
        IPHONEOS_DEPLOYMENT_TARGET: "8.0"
        PRODUCT_BUNDLE_IDENTIFIER: "io.XCEssentials.Toolbox"
        INFOPLIST_FILE: "Info/Fwk.plist"
        ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: "$(inherited)"
        SDKROOT: "iphoneos"
        TARGETED_DEVICE_FAMILY: "1,2"
        DEFINES_MODULE: "NO"
        SKIP_INSTALL: "YES"
variants:
  $base:
    abstract: true
  Main:
